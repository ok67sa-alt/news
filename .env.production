# Production Environment Variables for Frontend
# Leave empty to use same origin (recommended since frontend and backend are unified)

# API URL - leave empty to automatically use the current domain
VITE_API_URL=
